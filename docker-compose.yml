version: '3.8'

services:
  # mongo:
  #   image: mongo:6
  #   restart: always
  #   environment:
  #     MONGO_INITDB_DATABASE: flaskappdocker
  #     MONGO_INITDB_ROOT_USERNAME: root
  #     MONGO_INITDB_ROOT_PASSWORD: example
  #   volumes:
  #     - mongo-data:/data/db
  #   networks:
  #     - backend

  flask1:
    build: .
    environment:
      - MONGO_URI=mongodb+srv://mjdeleon02_db_user:GotlxoXkRgD0P0v7@flaskappdocker.vkjgmkx.mongodb.net/?appName=flaskappdocker
    depends_on:
    networks:
      - backend
    ports:
      - "5000:5000"

  flask2:
    build: .
    environment:
      - MONGO_URI=mongodb+srv://mjdeleon02_db_user:GotlxoXkRgD0P0v7@flaskappdocker.vkjgmkx.mongodb.net/?appName=flaskappdocker
    depends_on:
    networks:
      - backend
    ports:
      - "5000:5000"

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - flask1
      - flask2
    networks:
      - backend

volumes:

networks:
  backend:
    driver: bridge
